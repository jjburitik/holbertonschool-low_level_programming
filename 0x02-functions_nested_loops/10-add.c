#include "holberton.h"

/**
 *add - function that adds two integers and returns the result.
 *@a: number to add
 *@b: number to add
 *Return: the result a+b
 */
int add(int a, int b)
{
return (a + b);
}

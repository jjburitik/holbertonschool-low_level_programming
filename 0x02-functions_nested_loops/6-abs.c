#include "holberton.h"

/**
 *print_sign - function that computes the absolute value of an integer.
 *@n: number to check
 *Return: the absolute value of n
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
